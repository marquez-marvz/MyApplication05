package com.example.myapplication05

import android.content.DialogInterface
import android.content.Intent
import android.util.Log
import android.view.LayoutInflater
import androidx.appcompat.app.AlertDialog
import kotlinx.android.synthetic.main.answer_main.*

class _Keyboard {}

//Minimize: CTRL + SHFT + '-'
//Expand:   CTRL + SHFT + '+'
//cTRL Windows: Ctrl + Alt + L -- Code Indention
//Util.ZeroPad(num, 2)

//Move to the last cursor position: CTRL + ALT + left Arrow.

/*



    val dlgquiz = LayoutInflater.from(this).inflate(R.layout.util_confirm, null)
    val mBuilder =
        AlertDialog.Builder(this).setView(dlgquiz).setTitle("Do you want to import answer?")
    val inputDialog = mBuilder.show()
    inputDialog.setCanceledOnTouchOutside(false); //

    dlgquiz.btnYes.setOnClickListener {

    }
    dlgquiz.btnNo.setOnClickListener {
        inputDialog.dismiss()
    }


 */


/*

   var varBtnCountLate: Button? = null;


    val section = cboSectionActivity.getSelectedItem().toString();

    cursor!!.getString(cursor.getColumnIndex("BackupDate"))

            val db2: DatabaseHandler = DatabaseHandler(context)
              imgStudent22.setImageDrawable(null);


  val params: ViewGroup.LayoutParams = listAnswer.getLayoutParams()
                params.width = 180
                listAnswer.setLayoutParams(params)

 */

//
//override fun onBackPressed() {
//    Log.d("4356", "onBackPressed Called")
//    val intent = Intent(this, ActivityMain::class.java)
//    this.startActivity(intent)
//}

//
//override fun onCancel(p0: DialogInterface?) {
//    Log.e("Hello", "1111")
//}